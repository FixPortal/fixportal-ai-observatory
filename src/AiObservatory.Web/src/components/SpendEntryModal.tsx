import type { SpendCategory, SpendVendor } from '../api/client'

interface Props {
  categories: SpendCategory[]
  vendors: SpendVendor[]
  onClose: () => void
}

// Replaced wholesale in Task 8 — the real manual entry form.
export default function SpendEntryModal(_props: Props) {
  return null
}
