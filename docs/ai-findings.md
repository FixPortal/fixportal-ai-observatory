# AI Findings Ledger

Durable record of un-dismissable static-analysis findings (GitHub Code Quality,
CodeQL, Copilot AI Findings). Substitutes for the missing dismiss UI so the same
by-design issues do not get re-investigated on each scan.

| Finding | Status | Reason | Rationale | First seen |
|---|---|---|---|---|
| `src/AiObservatory.Web/src/components/DateRangePicker.tsx:47-48` — react-doctor/rerender-lazy-state-init | fixed | | Wrapped `toISOString()` calls in arrow-function initialisers so they run on mount only, not every render (PR #35) | 2026-06-23 |
| `src/AiObservatory.Web/package.json` — npm audit high: undici 7.0.0–7.27.2 (TLS bypass, cache poisoning, header injection, DoS) | fixed | | `npm audit fix` bumped undici past the vulnerable range (PR #35) | 2026-06-23 |
| `src/AiObservatory.Web/src/components/DateRangePicker.tsx:43` — react-doctor/prefer-useReducer (5 useState calls) | dismissed | by-design | Component uses the recommended prev-value tracking pattern (lines 53–60) to sync props→state without effects. The 5 state slots are independently updated; grouping into a reducer would obscure the derived-state guard without reducing complexity. | 2026-06-23 |
| `src/AiObservatory.Web/src/design/index.ts` — react-doctor/bundle-barrel-imports (bypassed barrel) | fixed | | All 8 consumer sites updated to direct imports; barrel file deleted (commit 4bd9251). Score 97→100. | 2026-06-23 |
| `src/AiObservatory.Web/src/components/SpendChart.tsx` — unstable Inner component identity inside lazy() factory | fixed | | Hoisted Inner to module scope via .then(); chart no longer remounts on mode/date toggle. | 2026-06-23 |
| `src/AiObservatory.Web/src/components/ProviderSplit.tsx` — unstable Inner component identity inside lazy() factory | fixed | | Hoisted Inner to module scope via .then(); chart no longer remounts on parent state changes. | 2026-06-23 |
| `src/AiObservatory.Web/src/pages/Dashboard.tsx` — accessibility/landmark-one-main: no <main> landmark | fixed | | Wrapped primary content region in <main class="dashboard__main">. | 2026-06-23 |
| `src/AiObservatory.Web/src/components/SummaryCards.tsx:71` — useMemo wrapping primitive .filter().length | fixed | | Dropped useMemo; inline filter().length is cheap enough not to memoize. | 2026-06-23 |
| `src/AiObservatory.Web/src/components/ModelBreakdown.tsx:160` — Math.max(...arr.map()) spread | fixed | | Replaced with arr.reduce() to avoid argument-count blowup on large datasets. | 2026-06-23 |
| `src/AiObservatory.Web/public/robots.txt` — SEO/robots-txt: no robots.txt | fixed | | Created public/robots.txt with Disallow: / (internal dashboard, must not be indexed). | 2026-06-23 |
| `src/AiObservatory.Web/src/components/SpendChart.tsx` — byDate useMemo includes mode dep: full reduce re-runs on chart toggle | open | | Split-memo opportunity (separate reduce from normalise pass) but micro-optimisation; data volume is small. Review if dataset grows. | 2026-06-23 |
| `src/AiObservatory.Web/src/components/SubscriptionPanel.tsx:178` — aggregates.filter() inside .map(): O(n*m) | open | | Build a per-provider Map instead; not a problem at current subscription count (<=5), revisit if provider cardinality grows. | 2026-06-23 |
| `src/AiObservatory.Web/src/components/*.tsx` — .sort() on fresh arrays vs .toSorted() idiom (BudgetRulesPanel, ModelBreakdown, SpendChart, SubscriptionPanel) | open | | Minor idiom preference; .sort() on a freshly-created array is safe. No correctness risk, defer to a style sweep. | 2026-06-23 |
| `src/AiObservatory.Web/src` — accessibility/color-contrast: Lighthouse flagged at least one element | open | | Candidate is --text-faint (#94a3b8) or --text-muted (#6b7280) on small text. Exact element unconfirmed without a live Lighthouse run. Investigate with DevTools before changing tokens. | 2026-06-23 |
| `src/AiObservatory.Api/Program.cs:104-106` — adversarial-review BG4: `dev/seed` TRUNCATEs all tables on every `docker compose up` | fixed | | Idempotency guard added — seed no-ops when `DailyAggregates` already has rows, so a re-up cannot wipe a self-hoster's data (batch1). | 2026-06-24 |
| `src/AiObservatory.Web/src/auth/msal.ts:61` — adversarial-review F1: viewer share key left in `?key=` URL | fixed | | `history.replaceState` strips `?key=` from the URL after capture; value kept in memory. Scope is read-only telemetry; API access logs never saw `?key=` (frontend URL only) (batch1). | 2026-06-24 |
| `src/AiObservatory.Api/Program.cs:55-57` — adversarial-review BG2: rate limiter buckets all anonymous Docker traffic under the nginx bridge IP | fixed | | `UseForwardedHeaders` + private-network `KnownNetworks` so the limiter partitions by the real client IP behind the trusted proxy (batch1). | 2026-06-24 |
| `docker-compose.yml:24` — adversarial-review F4: hardcoded default admin key `change-me` with no fail-fast | fixed | | Startup now throws outside Development if `OBSERVATORY_API_KEY` is unset or `change-me`; production pulls from Key Vault so prod was never exposed (batch1). | 2026-06-24 |
| `src/AiObservatory.Web/src/api/client.ts:22` — adversarial-review F3: read-only key sent on non-GET; write UI rendered for share-link viewers | fixed | | Not a security gap (backend `ApiKeyEndpointFilter` rejects all non-GET without the admin key); fixed as UX — `isReadonly` hides write controls in BudgetRulesPanel/InsightsFeed/SubscriptionPanel (batch1). | 2026-06-24 |
| `src/AiObservatory.Web/src/api/queries.ts:69` — adversarial-review BG3: `new Date()` in `usePriorPeriodAggregates` render body | fixed | | Wrapped the window calc in `useMemo([])`; render is pure (query key was already day-granular via `localDate`, so no refetch churn existed) (batch1). | 2026-06-24 |
| `src/AiObservatory.Api/ApiKeyEndpointFilter.cs:71-80` — adversarial-review F2: `FixedTimeEquals` early length-return leaks key length | dismissed | by-design | The length check is the structural prerequisite of `CryptographicOperations.FixedTimeEquals` (requires equal-length inputs). One bit/probe (byte-length equal?) on a 32+ char random Key Vault key is no usable oracle, swamped by network jitter. Verified Phase-4. | 2026-06-24 |
| `src/AiObservatory.Api/Program.cs:39` — adversarial-review BG1: `AnthropicIntelligenceClient` singleton thread-safety | dismissed | by-design | Both call paths build fresh `MessageParameters` per request with no shared mutable state; the SDK's `HttpClient` is thread-safe for concurrent sends. Standard supported singleton-wrapper pattern. Verified Phase-4. | 2026-06-24 |
| `src/AiObservatory.Web/src/components/DateRangePicker.tsx:47-60` — adversarial-review: props mirrored into local state | dismissed | by-design | Uses the React-sanctioned previous-prop-tracking-during-render pattern (guarded by `from !== prevFrom`), deliberately chosen to avoid setState-in-effect. Correct, not an anti-pattern. (Duplicate of the prefer-useReducer row above.) | 2026-06-24 |
| `src/AiObservatory.Ingest/Services/Anthropic/AnthropicUsageClient.cs:142` — ReSharper positional-property-never-accessed: `AnthropicUsageBucket.EndingAt` | dismissed | by-design | DTO mirrors Anthropic API response shape; field kept for schema fidelity though not consumed downstream. | 2026-07-08 |
| `src/AiObservatory.Ingest/Services/OpenAi/OpenAiUsageClient.cs:105` — ReSharper positional-property-never-accessed: `OpenAiUsageBucket.StartTime` | dismissed | by-design | DTO mirrors OpenAI API response shape; field kept for schema fidelity though not consumed downstream. | 2026-07-08 |
| `src/AiObservatory.Ingest/Services/OpenAi/OpenAiUsageClient.cs:105` — ReSharper positional-property-never-accessed: `OpenAiUsageBucket.EndTime` | dismissed | by-design | Same as above — DTO schema fidelity. | 2026-07-08 |
| `src/AiObservatory.Ingest/Services/OpenAi/OpenAiUsageClient.cs:111` — ReSharper positional-property-never-accessed: `OpenAiUsageResult.NumModelRequests` | dismissed | by-design | Same as above — DTO schema fidelity. | 2026-07-08 |
